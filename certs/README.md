# Certificados AWS IoT

Copiá aquí los archivos TLS que coincidan con los nombres definidos en `docker-compose.yml` (`AWS_IOT_CA_FILE`, `AWS_IOT_CERT_FILE`, `AWS_IOT_KEY_FILE`).

Esta carpeta se versiona vacía salvo este archivo; los certificados y claves **no** deben subirse al repositorio.
